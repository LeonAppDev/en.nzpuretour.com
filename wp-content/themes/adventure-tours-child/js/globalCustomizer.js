jQuery(document).ready(
function($)
{

var onSale = $('span.onsale');

//onSale.css({"display":"none"});


function stringToNumber(s)
{
  var number= Number(s);
  if(isNaN(number))
  return 0;
  else {
    return number;
  }
}


}
)
